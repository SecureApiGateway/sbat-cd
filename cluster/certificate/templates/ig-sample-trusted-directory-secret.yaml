---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.ig.sampleTrustedDirectory.secretName }}
spec:
  refreshInterval: 1h             
  secretStoreRef:
    kind: SecretStore
    name: gcp-store               
  target:
    name: {{ .Values.ig.sampleTrustedDirectory.secretName }}
    creationPolicy: Owner
  data:
  - secretKey: {{ .Values.ig.sampleTrustedDirectory.fileName }}
    remoteRef:
      key: {{ .Values.ig.sampleTrustedDirectory.googleSecretName }}
